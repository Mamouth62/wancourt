{% extends 'base.html.twig' %}

{% block title %}Wancourt - Education{% endblock %}

{% block body %}
    <div class="container mt-5 text-center">
        <h1 class="mb-4">Education</h1>
        <p class="mb-4">Cliquez sur l'article de votre choix pour lire le résumé</p>
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="table-responsive">
                    <table class="table table-bordered">
                        <tbody>
                            <tr>
                                <td class="article-summary">
                                    <h2 class="h4">RPI</h2>
                                    <a href="{{ path('app_rpi') }}" class="btn btn-info">Voir</a>
                                </td>
                                <td class="article-summary">
                                    <h2 class="h4">Réglementation</h2>
                                    <a href="{{ path('app_assistantes_maternelles') }}" class="btn btn-info">Voir</a>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <div class="container mt-5 text-center">
        <a href="{{ path('app_base') }}" class="btn btn-dark-blue mb-4">Retour à la Page Accueil</a>
    </div>
{% endblock %}
