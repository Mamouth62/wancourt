{% extends 'base.html.twig' %}

{% block title %}Wancourt -{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .text-center img {
            max-width: 100%;
            height: auto;
            display: block;
            margin: 0 auto;
        }
        .gallery-item {
            margin-bottom: 30px;
        }
    </style>
{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Feux de Végétaux, Potager</h1>
    
    <div class="row">
        <div class="col-md-6 mb-4">
            <div class="text-center gallery-item">
                {% set img1 = 'img/brulage_vert1.png' %}
                <img src="{{ asset(img1) }}" class="img-fluid" alt="Brûlage déchets vert 1">
            </div>
        </div>
        <div class="col-md-6 mb-4">
            <div class="text-center gallery-item">
                {% set img2 = 'img/brulage_vert2.png' %}
                <img src="{{ asset(img2) }}" class="img-fluid" alt="Brûlage déchets vert 2">
            </div>
        </div>
    <div class="container mt-5 d-flex justify-content-between">
        <a href="{{ path('app_arrete_communaux') }}" class="btn btn-dark-blue mb-4">Retour à la Arrêtés Communaux</a>
        <button onclick="window.print()" class="btn btn-dark-blue mb-4">Imprimer</button>
    </div>
</div>
{% endblock %}
